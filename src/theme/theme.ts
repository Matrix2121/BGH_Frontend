import { colors } from "./colors"
import { spacings } from './spacings';
import { typography } from './typography'; 

const theme = { colors, spacings, typography };

export default theme;
export { colors, spacings, typography };