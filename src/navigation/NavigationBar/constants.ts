/** Vertical extent of the center “bump” above the pill (unified mask silhouette). */
export const HANDLE_HEIGHT = 15;

/** Total animated height when the navigation bar is fully expanded (Figma / layout target). */
export const EXPANDED_NAV_TOTAL_HEIGHT = 320;
